# COM_STATE v0.4.1

STATUS: HISTORICAL PROJECTION / ROUTER — NOT CURRENT MUTABLE STATE
LAST FULL PROJECTION DERIVED: 2026-08-19 Europe/London BY FRAMEWORK
CURRENTNESS REPAIR: 2026-08-26 Europe/London

COM is a working coordination surface, not canon, validation, consensus or a truth oracle.

The former v0.4.0 body described a coherent 2026-08-19 projection but remained labelled `ACTIVE — CURRENT PROJECTION` after the live project moved materially. That label is now explicitly retired.

```text
RECORD_CORRECT_AT_T0 != RECORD_CURRENT_AT_T1
COHERENT_PROJECTION != CURRENT_WORLD
HISTORICAL_STATE != ABSENT_STATE
```

The exact pre-repair projection remains preserved in Git as blob:

`a541dfe1326531f4477ee043738389f9cd77f9e4`

Do not reconstruct it into current state by memory. Retrieve it only when historical 2026-08-19 evidence is material.

## CURRENT ROUTING ORDER

For a fresh Framework runtime:

```text
RELOAD.md
-> continuity/BOOT.md
-> continuity/FRAMEWORK_HEAD.md
-> continuity/OMISSION_MAP.md only where material
-> live reacquisition of mutable sources
-> act
```

For current project coordination:

- bounded cross-project orientation: `continuity/FRAMEWORK_HEAD.md`;
- negative-space retrieval triggers: `continuity/OMISSION_MAP.md`;
- TRACE working integration: TRACE PR #38 + named PROJECT evidence;
- TRACE exchange/evidence ledger: COM issue #46;
- Campfire Square field ledger: COM issue #42;
- project-plan x100/drift evidence: COM issue #56;
- live Square state: bounded read/preflight only; old ledger state is not current capability;
- Mechanical Ethics current state: live ME repository / PR #34 when material;
- Campfire Relay current implementation: live PR #190 / WIP map when material.

```text
SUMMARY != SOURCE
LEDGER != HEAD
HEAD != WORLD
PAST_CAPABILITY != CURRENT_CAPABILITY
```

## CURRENT EXECUTION POINTER — 2026-08-26 AFTERNOON

Fresh reacquisition found no overnight movement on the primary repository lanes.

Current TRACE outward cycle remains:

```text
RAIB 5 + PAC 5 + EPA 5 + NHTSA 0 = 15 / 20
```

The two official NHTSA archive CSVs were downloaded and hashed locally on 2026-08-25, but the CSVs plus `NHTSA_SGO_TRANSPORT_RECEIPT.json` have not entered the Framework study aperture. Exact in-aperture byte/hash verification is therefore still the next build boundary.

Fresh Square HEAD request `fw-comsync-20260826T1518Z-head-001` was delivered to the GitHub read-relay carrier on 2026-08-26; no matching bounded-read response had been observed at this COMSYNC. Therefore current Square board/quota/grant/obligation state is UNKNOWN rather than quiet.

```text
READ_REQUEST_DELIVERED != READ_RESPONSE_OBSERVED
NO_RESPONSE_YET != NOTHING_CHANGED
```

See `continuity/FRAMEWORK_HEAD.md` for the bounded current snapshot and exact next executable boundary.

## RULE

Do not turn this file back into a second detailed current-state authority. If a future projection is needed, either derive it explicitly from named live dependencies or keep it visibly historical/unknown.

`The lullaby was never for the cradle`.
