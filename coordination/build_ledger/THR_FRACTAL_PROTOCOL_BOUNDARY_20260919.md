# THR fractal RFC — protocol boundary

Date: 19 September 2026 — Europe/London

Status: CURRENT RFC PRESSURE RECEIPT / NOT CANON / PUBLIC THR UNCHANGED

## Exact state

THR draft PR #52:
- head `3d348e17afa3dfcf4e420db98de369846e77fab8`
- DRAFT / OPEN / MERGEABLE
- hosted integrity run 145 / `35466043388` SUCCESS

Public THR:
- main `1f5a5919938f385f43f1e2383bdfbb52807b206e`
- exactly 4 public records
- no registry-semantics change
- record 5 not earned

## New subtraction

The zero-new-shared-types result created a further question:

> Does THR need a new protocol at all?

The live repository already has `RECORD_CONTRACT.md`:
- working interoperability note
- derived from current records
- not a universal schema
- not canon

Its existing rule:

```text
COMMON QUESTIONS != IDENTICAL OBJECTS
RECORD CONTRACT != UNIVERSAL ONTOLOGY
STRUCTURE != TRUTH
```

This matches the surviving RFC better than a new universal protocol.

Application-profile practice provides a mature precedent for reusing semantics from
multiple base standards and adding local application requirements. THR is not claiming
conformance to a particular application-profile standard.

## Current provisional architecture ceiling

```text
THR UNIVERSAL ONTOLOGY = NOT EARNED
THR PROTOCOL = NOT EARNED

CURRENT ARCHITECTURE
= PUBLIC RECORD PRODUCT
+ MINIMUM INTEROPERABILITY CONTRACT
+ SPARSE CROSS-RECORD REFERENCES WHERE EARNED
+ RECORD-LOCAL STRUCTURE
+ STRONGER-OWNER MAPPINGS WHERE MATERIAL
+ EXPLICIT EVIDENCE / CORRECTION / RIGHTS CEILINGS
```

A future protocol should be earned by a concrete multi-implementation exchange
requirement that existing formats, mappings, application profiles or stronger-owner
standards cannot preserve.

```text
ONE IMPLEMENTATION != PROTOCOL NEEDED
PORTABILITY GOAL != NEW PROTOCOL NEEDED
INTEROPERABILITY != OWN EVERY SEMANTIC
```

## Falsification route

The independent review packet now asks for:

```text
PROTOCOL_NECESSITY:
<concrete current requirement, or NO PROTOCOL PRESSURE FOUND>
```

It also continues to attack:

```text
NEW SHARED THR CORE TYPES REQUIRED NOW = ZERO
```

A concrete counterexample may still earn the smallest missing type or relation.

## Current stop

Do not build:
- a universal THR ontology
- a THR wire protocol
- a custom archival package
- a custom federation/consensus layer
- new global object types

unless real use earns one.

```text
SMALLER ARCHITECTURE != SMALLER PURPOSE
INTEGRATION VALUE != ONTOLOGY NOVELTY
NO PROTOCOL PRESSURE -> KEEP CONTRACT / PROFILE-LIKE FORM
```
