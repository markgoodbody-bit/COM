# NVIDIA Claw EvidenceWatch — preflight-green source freeze — 19 September 2026

Status: **ACTIVE COMPETITION BUILD / SOURCE CORE GREEN / DRAFT PR / LIVE NVIDIA WITNESS NEXT / NOT SUBMITTED**

Candidate:
**EvidenceWatch**

> EvidenceWatch tells you when the evidence underneath an important claim materially changes after you have already relied on it — and which downstream decision/report should be revisited.

Relay draft PR:
`#248`

Exact source head:
`f4c5a51850b6e183c7718715f2e6a0cc6a80f4d6`

Hosted:
`campfire-ci 1563 / 35448429677 — SUCCESS`

Focused competition tests:
**15** (14 core + 1 browser HTTP E2E), with root CI importing the exact same sources.

## Current source core

- durable append-only ledger;
- scheduled heartbeat runner;
- live HTTP fetcher;
- exact observation dedupe;
- NVIDIA Build OpenAI-compatible analyzer;
- current official Nemotron 3 Super hosted model default;
- request sampling aligned with current NVIDIA model guidance;
- explicit state-authority boundary;
- model analysis != canonical claim authority;
- derivative repeat quiet;
- derivative/supporting divergence visible without canonical overwrite;
- primary source unreachable alert with last-known state preserved;
- bounded same-origin discovery with deterministic candidate extraction;
- model-invented discovery URLs rejected;
- discovered sources non-authoritative by default;
- downstream dependent decisions/reports carried into material alerts;
- deterministic browser/CLI demo;
- real HTTP browser E2E;
- restart state reconstruction;
- credential-safe one-call NVIDIA inference probe ready.

## Strong-owner subtraction

Current external owner comparison narrows claims:
- Visualping: scheduled page monitoring + AI important-change classification;
- Distill: durable page change history/conditional alerts;
- Crossmark: formal scholarly correction/retraction/update status;
- scite: monitoring supporting/contrasting citations + retractions;
- NVIDIA/OpenShell/NemoClaw: long-running runtime/persistence/recovery.

Therefore EvidenceWatch does **not** claim novelty for those layers.

Current product hypothesis only:

```text
POST-RELIANCE CLAIM MONITORING
+ CROSS-SOURCE SEMANTIC STATE
+ SOURCE ANCESTRY / INDEPENDENCE
+ EXPLICIT CANONICAL SOURCE AUTHORITY
+ CORRECTION MEMORY
+ MATERIAL-DELTA FILTERING
+ DOWNSTREAM "WHAT MUST I REVISIT?" DEPENDENCY
```

This remains a hypothesis, not novelty proof.

## Current gate

The next highest-value evidence is a real NVIDIA Build call.

Branch contains:
`competition/nvidia-claw-evidencewatch/docs/LIVE_PROBE.md`
and
`npm run probe`.

Credential rule:
- Mark generates/copies a legitimate NVIDIA Build API key locally;
- **do not paste the API key into ChatGPT, GitHub, COM, screenshots or issues**;
- run probe locally with env-only credential;
- return sanitized console output only.

Current official NVIDIA source confirms:
- model `nvidia/nemotron-3-super-120b-a12b` is currently available on a free hosted endpoint;
- OpenAI-compatible base route is `https://integrate.api.nvidia.com/v1`;
- model guidance recommends temperature 1.0 / top_p 0.95.

## Still not established

- live NVIDIA inference witness;
- unattended live source run;
- public deployment;
- OpenClaw/NemoClaw packaging need;
- user demand;
- exact London T&Cs;
- submission.

```text
SOURCE GREEN = YES
LIVE NVIDIA WITNESS = NO
SUBMISSION = NO
RELAY MAIN / PRODUCTION = UNCHANGED
```

Do not add more features before live-provider evidence unless a concrete source defect is found.
