# Portfolio / token-allocation receipt — 18 September 2026 late

Status: **DATED PORTFOLIO ROUTING RECEIPT / NOT PROJECT PURPOSE / NOT BUILD QUEUE / NOT AUTHORITY EXPANSION**

Basis:
- direct Mark instruction: check all current projects and choose where Framework's tokens are best used;
- live COM main at start of allocation pass: `28a26d2267499e44437240cc7a0bb67fb060333a`;
- fresh GitHub repository / issue / PR / current project-entry inspection.

This receipt records the allocation decision and its stop condition. It does not make the chosen instrument the new project centre.

## Purpose

```text
HOW CAN WE MAKE A BETTER FUTURE?

WORLD / REAL USE
-> STRONGEST OWNER
-> SPECIFIC CONSEQUENTIAL GAP
-> SMALLEST HELP
-> WATCH CONSEQUENCES

PURPOSE > INSTRUMENT
```

## Portfolio read

### TRACE

Fresh repository state:
- current main `e7d46398dc00ead931b0d5cae98518c1bcf304a3`;
- released formal baseline remains v0.3.0;
- release remains NOT VALIDATED / NO EFFICACY RESULT;
- no open implementation PR;
- external-criticism issue #52 has no fresh result that earns a semantic patch.

Disposition:

```text
TRACE = STABLE BASELINE
NEW PRIMITIVE = NO
NEW EFFICACY PROGRAMME = NO
TOKENS NOW = NO
```

### Mechanical Ethics

Fresh repository state:
- current main `714907a4d0af7bd702b0ab92786aa858213812b4`;
- released formal baseline remains v0.7.0;
- issue #47 reader-pressure harness is ready;
- no uncontaminated Condition-B cold-reader return exists;
- current Framework/Codex apertures are contaminated for that test.

Disposition:

```text
ME = STABLE BASELINE
COLD READER REQUIRED
AVAILABLE EXPOSED APERTURE != VALID TEST SUBJECT
TOKENS NOW = NO WORDING PATCH
```

### The Human Record

Fresh repository state at portfolio pass:
- main `1f5a5919938f385f43f1e2383bdfbb52807b206e`;
- public catalogue = exactly four records;
- outside R. Vale case earned bounded identity/assertion repairs;
- remaining candidate-history seam is explicitly held because no real-use failure has yet shown that Git/assertion supersession is insufficient;
- record 5 remains unearned;
- direct contribution packet route is present; public-byte readback remained UNKNOWN in this Framework aperture because the available network routes failed.

Disposition:

```text
THR = MAINTAIN / REAL BREAKAGE
RECORD 5 = NOT EARNED
TYPED CANDIDATE HISTORY = NOT EARNED
FAILED FRAMEWORK FETCH != PUBLIC FAILURE
TOKENS NOW = NO SCHEMA / RECORD GROWTH
```

### Please Start From Here

Current coordinated public state:
- D072 remains the last byte-verified current public delivery;
- four-record THR currentness was repaired across the linked surfaces;
- no redesign is currently earned.

Leave a Mark #118:
- Remark42 + small PSFH adapter + restore guard = technically plausible;
- public intake still sits behind explicit human/controller/legal/production-topology choices;
- no amount of repository token churn can honestly substitute for those choices.

Disposition:

```text
PSFH = OBSERVE / CORRECT
LEAVE A MARK = HUMAN-GATED
MORE ARCHITECTURE != PROGRESS
TOKENS NOW = NO
```

### Formation Under Uncertainty

Current entry surface remains:
- working / non-production / not canon / not alignment solved;
- Formation Environment v0.2 current candidate;
- Reciprocal Formation remains a separate relationship-level architecture;
- fresh 2026 skill-formation evidence grounds existing dependency / skill-retention structure rather than earning a new concept.

Disposition:

```text
FORMATION = OWNER-GROUNDED CANDIDATE
CARE_EXPRESSED != CARE_INTERNALIZED
NEW VERSION / BENCHMARK = NOT EARNED
TOKENS NOW = NO
```

### Open Horizon

Current role remains discoverability / expansion map:
- not canon;
- not backlog;
- not TRACE schema;
- not an obligation to explore every territory.

Disposition:

```text
OPEN HORIZON = MAP / ROUTER
MISSING ROW != BUILD TASK
TOKENS NOW = ONLY WHEN WORLD QUESTION FIRES
```

### Answerable Construction

Current front door explicitly records:
- contribution not established;
- stronger owners absorbed most substantive mechanisms;
- remaining cross-owner question is not yet operationalised as a falsifiable test.

Disposition:

```text
ANSWERABLE CONSTRUCTION = PRESERVED SYNTHESIS / OPEN QUESTION
OPEN QUESTION != CONTRIBUTION
TOKENS NOW = NO NEW LAYER
```

### Field / world cases

The field directory already contains many positive, adverse, material, ecological, institutional, economic and long-time witnesses.

The selection rule remains:
- real mechanism pressure;
- strong-owner subtraction;
- positive construction;
- hard collision;
- or dated future evidence gate.

The MV Matui case was the latest earned addition immediately before this pass.

Disposition:

```text
FIELD != CASE COLLECTION GAME
NO CATEGORY QUOTA
NEXT CASE MUST BE EARNED
```

### ATRS / competitions

ATRS #364:
- September method/calibration result preserved;
- population result intentionally uncomputed;
- wake on fresh November corpus/rules, concrete falsifier, or exact stronger owner.

Hack Apertus:
- pre-event preparation preserved;
- result surface held until the 1 October challenge window/rules are live.

Open Agent / Nebius:
- no current product is earned after EvidenceBridge / BeforeBuild owner subtraction.

Resources:
- no current surviving work is sufficiently blocked on paid inference to earn an application.

Disposition:

```text
COMPETITION EXISTS != BUILD EARNED
PRE-CONSUME RESULT SURFACE = NO
TOKENS NOW = HOLD
```

### External-result watches

Two Parents:
- project-side build/submission work is complete;
- current value is owner result, not more local construction.

llms.txt hub:
- upstream PSFH PR #1648 and THR PR #1649 remain open / mergeable / awaiting owner review;
- chasing the owner adds no evidence.

Disposition:

```text
SUBMITTED != ACCEPTED
OWNER RESULT WATCH != BUILD LANE
TOKENS NOW = NO
```

### Legacy ancestors

Fresh repository inspection:
- `universal-codex`;
- `monolith`;
- `eidoran`.

These contain important ancestry but no current live route outranking the released/current ME / TRACE / COM programme. No open work was found that justifies reviving them as active centres.

Disposition:

```text
ANCESTRY != CURRENT QUEUE
ARCHAEOLOGY ONLY WHEN CURRENT DEFECT POINTS BACK
TOKENS NOW = NO
```

Long-form/book/paper artifacts not exposed by the current live routing were not promoted into a build lane merely because they exist.

### COM

COM remains coordination/continuity infrastructure.

The recent drift repair already showed the danger of letting continuity surfaces become history buses.

Disposition:

```text
COM = ROUTE / RECEIPT / COORDINATION
COM != PROJECT PURPOSE
NO NEW GENERAL QUEUE
NO HEAD EXPANSION FROM THIS PASS
```

## Chosen token allocation: Campfire Relay — bounded source reliability

This was the one live surface with all of:

```text
CONCRETE OBSERVED DEFECTS
+ CROSS-PROJECT CONSEQUENCE
+ SMALL REVERSIBLE SOURCE REPAIRS
+ DIFFERENTIATED HOSTILE REVIEW
+ NO NEED TO CROSS HUMAN GATE
```

Why it outranked another theory/product/case:

1. Relay / COMSYNC is a force multiplier for future AI-to-AI collaboration across the project.
2. Real failures already existed:
   - session-only debt/sync machinery unavailable to successors;
   - cached worker state could imply current health;
   - repeated request IDs could vanish as silent no-ops;
   - COMSYNC Square reads could overrun limits, hide replies or regress cursors;
   - legacy offline tests had become known-red and therefore stopped protecting the instrument.
3. Repairing these defects reduces Mark's human relay burden without asserting that Relay itself is the purpose.
4. The work is repository-source-only and does not require credentials, spend, external institutional contact, local installation, scheduler activation or Production promotion.

## Work completed in this allocation pass

### Main test substrate

PR #241 merged to Campfire Relay main as:

`32143937d6a642a6f5e2404d368fd09aa4d54da9`

Result:
- test HTTP-port selection now rejects ports the actual HTTP client will block;
- production behavior unchanged;
- exact hosted `campfire-ci` and `budget-core-ci` were green.

### Maintained Simple-v1 source

PR #240 integrated into `framework/campfire-square-simple-v1` as:

`b2c31c258bf6547f6e0324068428aa1699943cc6`

Result:
- cached worker observations expose their timestamp/age;
- current health remains explicitly unverified.

PR #243 integrated as:

`9ae901f9414e7f6e00ea37f9965662132f2dd23e`

Result:
- duplicate `request_id` -> explicit `DEDUPED_NO_OP`;
- no second send;
- original receipt not overwritten;
- separate dated no-op receipt;
- dedicated regression added to hosted Simple-v1 CI.

Old PR #238 was closed as superseded by the current-head integration.

### Watchdog / COMSYNC development source

PR #239 integrated as:

`3b20b419d10d4ed3efd89e0db067e8b1b39e7df0`

It preserved the live Square/COMSYNC instrument in repository source and, after repeated Codex/Claude attack, now includes:
- derived watched-thread/addressed-thread discovery rather than a hand list;
- keyed answer/disposition semantics;
- explicit candidate hints rather than mention-as-answer;
- visible dismissals;
- bounded delta/cursor state;
- fail-closed thread completeness;
- first-429 stop;
- request-attempt budget;
- no regressing cursor persistence;
- GitHub budget/currentness visibility;
- installed-vs-source observations.

Concurrent Claude cleanup at:
`604d6365ea1a77d0fde46412d02dbfe797cfedb5`
closed two test file handles identified by Codex.

PR #244 then integrated as:

`57dd394d64eda71e853e50087c545bd1378bbeda`

Result:
- installed-vs-source speech-worker check follows the maintained Simple-v1 source rather than closed PR #238;
- stale `test_fetch_integrity.py` and `test_install_observation.py` harnesses were repaired;
- all `tools/comsync/test_*.py` now run inside the existing `campfire-ci` job rather than creating another workflow/job;
- first hosted run correctly exposed the stale fixtures;
- repaired run:
  - COMSYNC offline regressions = SUCCESS;
  - normal npm test = SUCCESS.

## Explicit boundary

Nothing in this pass crossed the local/runtime gate.

```text
SOURCE_REPAIRED
!= INSTALLED
!= RUNNING
!= REBOOT-SURVIVING
!= PRODUCTION
```

PR #209's exact local-machine phrases remain authoritative:
- `install-watch`;
- `install-and-enable`;
- `start-once`.

Mark's instruction to choose useful token work did not use one of those exact phrases.

Therefore:
- no watchdog install;
- no scheduled-task enablement;
- no service start;
- no credential use;
- no Square write;
- no Production promotion.

Likewise, the R29/R30 authority lane remains human/authority-sensitive and was not adopted by momentum.

## Token stop condition

The reason for choosing Relay was to make future collaboration/continuity more trustworthy, **not to turn Relay maintenance into the project**.

The bounded repair objective is now satisfied at source level:

```text
REAL DEFECTS REPAIRED
+ HOSTILE COUNTEREXAMPLES REGRESSED
+ CONTINUITY TOOL REPOSITORY-RESIDENT
+ OFFLINE REGRESSION FLOOR IN CI
+ HUMAN RUNTIME GATE PRESERVED
```

So the next token allocation should return to:

```text
WORLD / REAL USE
-> STRONGEST OWNER
-> CONSEQUENTIAL RESIDUE
-> SMALLEST HELP
```

unless:
- a fresh Relay defect breaks coordination again;
- Mark explicitly crosses a local/runtime gate;
- a real external return lands on a current project;
- a time-gated competition/research window opens.

## Compression

> The highest-leverage work was not another theory, record, competition entry or site feature. It was making the project's AI collaboration and continuity machinery less able to silently lose state, hide debt or imply health it had not observed. That source-level repair is now bounded and tested. Stop polishing the plumbing and return to the world.
