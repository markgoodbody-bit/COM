# EvidenceWatch — live NVIDIA inference witness — 19 September 2026

Status: **REAL NVIDIA PROVIDER WITNESS PASS / ONE BOUNDED CALL / LIVE WEB HEARTBEAT NEXT**

Mark ran the branch's credential-safe PowerShell probe locally.

Exact local branch head observed:
`ad2b720447626f5074f64a88255a1dbbc49df47d`

Local runtime:
- Node `v24.19.0`;
- npm `11.17.0`.

Provider/model:
- NVIDIA Build hosted prototype endpoint;
- `nvidia/nemotron-3-super-120b-a12b`.

Observed returned structured analysis:

```json
{
  "proposition": "The owner publicly identified four real-world cybersecurity evaluation incidents.",
  "status": "supported",
  "quantity": "4 incidents",
  "scope": "later review",
  "searchAperture": "substantially broadened search aperture",
  "relation": "correction",
  "summary": "Later primary assessment identifies four incidents, correcting prior count of three.",
  "materialReasons": [
    "Incident count updated from 3 to 4",
    "Search aperture broadened beyond initial 141,006 runs"
  ]
}
```

Probe terminated with:
`NVIDIA_PROBE_OK`

Credential handling:
- key entered through secure local prompt;
- key not present in transcript supplied to Framework;
- no credential stored in COM/GitHub/chat.

Evidence ceiling:

```text
REAL NVIDIA ENDPOINT REACHED = YES
REAL NEMOTRON RESPONSE = YES
STRUCTURED ANALYSIS PARSED = YES
ONE BOUNDED INTEROP WITNESS = PASS

CLASSIFICATION ACCURACY = NOT ESTABLISHED
REPEATABILITY = NOT ESTABLISHED
LIVE WEB HEARTBEAT = NOT YET WITNESSED
UNATTENDED LONG-RUN = NOT YET WITNESSED
SUBMISSION = NO
```

Next:
1. run a public-source live heartbeat using fixed non-sensitive sources;
2. preserve source fetch + analysis + ledger evidence;
3. restart and confirm durable state recovery;
4. only then decide whether OpenClaw/NemoClaw packaging adds judging value.
