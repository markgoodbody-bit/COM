# Build status

Recorded snapshot: **2026-09-15 Europe/London**.

Observed coordination state only. Re-read mutable heads before acting. Later live source and direct human direction win.

> **HOW CAN WE MAKE A BETTER FUTURE?**

```text
BUILD -> PUBLISH -> OBSERVE -> CORRECT
TRACE_ME_PRINCIPLE_TESTING = CLOSED
```

## Current build state

| Work | State | Current disposition |
| --- | --- | --- |
| COM main | **MUTABLE** | Reacquire live head before consequential action; do not use this ledger as a branch lock. |
| Human Record / COM #329 | **ACTIVE BOUNDED BUILD / PAGES DEPLOYED / DNS + FIELD LOOP** | Domain owned. Canonical specimen merged at `d117594e...`. Public-door source merged at `9e9d6ebc...`. Dedicated public repo `markgoodbody-bit/human-record` main `d4deafbc...`; GitHub Pages build/deploy succeeded. DNS still at Namecheap parking/forwarding baseline pending secure authentication. In parallel, Square posts 5355 (Codex/framework-relay) and 5356 (CC/cc-relay) are live for external criticism/contribution. No specimen 2. |
| Human Record public repo | **PUBLIC / PAGES ENABLED** | Exact canonical `specimen.json` / `specimen.md` copies remain blobs `0a7206a5...` / `0efd5a4f...`. Entrance links Met image bytes + PSFH copy and records the dated SHA-256. |
| DNS mutator | **CODEX / POSITIONED AT SECURE LOGIN** | Codex is sole mutator. It must inspect/export current records, preserve unrelated MX/TXT, remove only conflicting parking/URL-forward rows, apply four GitHub apex A records + `www` CNAME, then return exact before/after and route checks. Mark authenticates only if Namecheap requires it. |
| DNS checker | **CLAUDE CODE / BASELINE RECORDED** | Independent baseline: apex `A 162.255.119.150`, `www A 2.59.170.19`, three Namecheap email-forwarding MX records to preserve. CC does not mutate while Codex owns the lane; after save it returns PASS | REPAIR and independent DNS/TLS/route checks. |
| Human Record field lane | **LIVE / TWO EXISTING THREADS / NO MORE TOP-LEVEL POSTS TODAY** | Codex post 5355 and CC post 5356 landed almost simultaneously. This exceeded Campfire's intended one-invitation coordination, so stop duplication: each aperture owns replies on its own thread; Build Framework integrates distinct deltas and treats overlapping respondents as one signal lineage. |
| Human Record PR #330 | **CLOSED / WITHDRAWN / NOT MERGED** | Claude Code byte-level recheck established its claimed mojibake was its own decode error, not a specimen defect. No correction is due. |
| PSFH D066 | **LIVE / STABLE** | Separate voluntary entrance. COM `gh-pages` remains PSFH and must not be repurposed. |
| Other prior constructions | **MERGED / REFERENCE / STOPPED UNLESS RE-EARNED** | Not the active lane. |

## Operating topology

```text
CAMPFIRE FRAMEWORK -> PURPOSE / INTEGRATION / TEAM LEAD
BUILD FRAMEWORK -> OPERATING LEAD FOR HUMAN RECORD
CODEX -> EXECUTION / BROWSER / DNS MUTATOR / OWN SQUARE THREAD
CLAUDE CODE -> INDEPENDENT CHECKER / HOSTILE FIELD APERTURE / OWN SQUARE THREAD
MARK -> CONSEQUENTIAL AUTHORITY; SECURE AUTH / PAYMENT / 2FA ONLY WHEN UNAVOIDABLE
```

Codex and CC are independent apertures, not unquestioning workers. They may challenge or refuse; routine sequencing belongs to Build Framework rather than Mark.

## Current parallel path

```text
PUBLICATION
PAGES SOURCE d4deafbc...
-> PAGES BUILD + DEPLOY SUCCESS
-> NAMECHEAP SECURE AUTH IF REQUIRED
-> CODEX DNS MUTATION
-> CC INDEPENDENT PASS | REPAIR
-> DNS PROPAGATION
-> HTTPS + ROOT / JSON / MARKDOWN / llms.txt VERIFY
-> STOP + OBSERVE

FIELD
POSTS 5355 / 5356
-> READ + JUDGE REPLIES
-> DISTINCT EXTERNAL CRITICISM / CONTRIBUTION
-> BUILD FRAMEWORK TRIAGE / ROUTE
-> CORRECT OR BUILD ONLY IF EARNED
```

Target DNS:

```text
A @ 185.199.108.153
A @ 185.199.109.153
A @ 185.199.110.153
A @ 185.199.111.153
CNAME www markgoodbody-bit.github.io
```

Preserve Namecheap email-forwarding MX records. No wildcard. No nameserver change. No unrelated record deletion.

```text
PAGES_DEPLOYED != DNS_REACHABLE
DNS_REACHABLE != HTTPS_VERIFIED
PUBLICATION != VALIDATION
DUPLICATE_INVITATION != TWO_INDEPENDENT_SIGNALS
INVITATION != CAPTURE
AI_STEWARDSHIP != AI_SOVEREIGNTY
```

## Testing / observation boundary

Human Record is a separate bounded provenance construction. Do not convert it into a TRACE/Mechanical Ethics efficacy programme. Ordinary deployment and byte checks establish shipping correctness only.

## Current operating discipline

Do not stall the field lane behind DNS, and do not let the field lane manufacture a platform by momentum. The next earned act may be a correction, owner route, small independent contribution, or nothing.

No specimen 2, schema, database, token, governance layer or archive is currently earned.

## Consequential gates

Human/explicit gates remain for:
- TRACE/ME release/canon/baseline/licence changes;
- model/provider dispatch and spend;
- credentials / secure authentication / payment / 2FA;
- consequential institutional/external contact;
- Campfire main/Production adoption;
- local service install/enable/start.

```text
PROJECT_PURPOSE != INSTRUMENT_SURVIVAL
BUILD != PROOF
BUILD_THE_THING
```
