# Build status

Recorded snapshot: **2026-09-14 Europe/London**.

Observed coordination state only. Re-read mutable heads before acting. Later live source and direct human direction win.

> **HOW CAN WE MAKE A BETTER FUTURE?**

```text
BUILD -> PUBLISH -> OBSERVE -> CORRECT
TRACE_ME_PRINCIPLE_TESTING = CLOSED
```

## Current build state

| Work | State | Current disposition |
| --- | --- | --- |
| COM main | **MUTABLE** | Reacquire live head before consequential action; do not use this ledger as a branch lock. |
| Human Record / COM #329 | **ACTIVE BOUNDED BUILD / PAGES DEPLOYED / DNS GATE OPEN** | Domain owned. Canonical specimen repaired and merged at `d117594e...`. Public-door source merged at `9e9d6ebc...`. Dedicated public repo `markgoodbody-bit/human-record` main `d4deafbc...`; GitHub Pages workflow and deploy job succeeded. Codex observed Namecheap parking DNS still at `162.255.119.150`. DNS/HTTPS not yet verified. No specimen 2. |
| Human Record public repo | **PUBLIC / PAGES ENABLED** | `has_pages: true`. Exact canonical `specimen.json` / `specimen.md` copies remain blobs `0a7206a5...` / `0efd5a4f...`. Entrance also links the Met image bytes + PSFH copy and records their dated SHA-256. |
| Human Record PR #330 | **CLOSED / WITHDRAWN / NOT MERGED** | Claude Code byte-level recheck established its claimed mojibake was a decode error in CC's own comparison, not a defect in the record. No correction is due. |
| PSFH D066 | **LIVE / STABLE** | Maintained `404de6f0...`, public `0566ef9a...`. COM `gh-pages` remains PSFH and must not be repurposed. |
| Reciprocal Delegation fan-out | **MERGED / STOPPED** | Existing reference work remains available; no further growth currently earned. |
| Other positive/world constructions | **MERGED / REFERENCE** | Not the active lane. |

## Human Record current edge

```text
PAGES SOURCE d4deafbc...
-> PAGES BUILD SUCCESS
-> PAGES DEPLOY SUCCESS
-> NAMECHEAP PARKING DNS STILL PRESENT
-> AUTHENTICATED DNS CHANGE REQUIRED
-> DNS PROPAGATION
-> HTTPS + ROUTE VERIFY
-> OBSERVE
```

GitHub Pages' current published apex configuration is:

```text
A @ 185.199.108.153
A @ 185.199.109.153
A @ 185.199.110.153
A @ 185.199.111.153
```

Optional/recommended `www`:

```text
CNAME www markgoodbody-bit.github.io
```

Remove conflicting parking/apex `A` / `AAAA` / `ALIAS` / `ANAME` records before expecting certificate issuance. GitHub notes HTTPS can take up to about an hour after correct DNS configuration.

```text
PAGES_DEPLOYED != DNS_REACHABLE
DNS_REACHABLE != HTTPS_VERIFIED
PUBLICATION != VALIDATION
```

## Testing / observation boundary

Human Record is a separate bounded provenance construction. Do not convert it into a TRACE/Mechanical Ethics efficacy programme.

Only live TRACE/Mechanical Ethics model observation lane remains:

`AI + https://pleasestartfromhere.com/ -> NATURAL RESPONSE`

## Current operating discipline

Finish the first Human Record public door before creating another specimen, schema or platform.

## Consequential gates

Human/explicit gates remain for:
- TRACE/ME release/canon/baseline/licence changes;
- model/provider dispatch and spend;
- credentials;
- authenticated registrar/DNS account actions not exposed to current tooling;
- consequential institutional/external contact;
- Campfire main/Production adoption;
- local service install/enable/start.

```text
PROJECT_PURPOSE != INSTRUMENT_SURVIVAL
BUILD != PROOF
BUILD_THE_THING
```
