# Build status

Recorded snapshot: **2026-09-15 Europe/London**.

Observed coordination state only. Re-read mutable heads before acting. Later live source and direct human direction win.

> **HOW CAN WE MAKE A BETTER FUTURE?**

```text
BUILD -> PUBLISH -> OBSERVE -> CORRECT
TRACE_ME_PRINCIPLE_TESTING = CLOSED
```

## Current build state

| Work | State | Current disposition |
| --- | --- | --- |
| COM main | **MUTABLE** | Reacquire live head before consequential action; do not use this ledger as a branch lock. |
| Human Record / COM #329 | **ACTIVE / HOLDING PAGE LIVE IN PAGES BUILD / DNS + FIELD LOOP** | Domain owned. Canonical specimen remains `d117594e...`. Public repo current head `713f464c...` is a holding-page-only `index.html` change and Pages build succeeded. DNS is still at Namecheap parking/forwarding baseline pending secure authentication. Square 5355/5356 remain the live field apertures. No specimen 2. |
| Human Record public repo | **PUBLIC / PAGES ENABLED / HEAD `713f464c...`** | Run `34907891851` succeeded on exact head. Canonical `specimen.json` / `specimen.md`, `llms.txt` and CNAME are unchanged by the holding-page edit. |
| DNS mutator | **CODEX / POSITIONED AT SECURE LOGIN** | Sole mutator. After authentication, inspect/export current records, preserve email-forwarding MX + unrelated TXT, remove only conflicting parking/URL-forward rows, apply four GitHub apex A records + `www` CNAME, then return exact before/after and route checks. |
| DNS checker | **CLAUDE CODE / BASELINE RECORDED** | Independent baseline: apex `A 162.255.119.150`, `www A 2.59.170.19`, three MX records to preserve. CC does not mutate while Codex owns the lane; after save it returns PASS | REPAIR and independent DNS/TLS/route checks. |
| Field delivery transport | **RECOVERY ORDERED / EXISTING SUPERVISOR STOPPED** | Codex observed installed speech supervisor STOPPED at `2026-09-14T22:47:59.8517011Z`; queued aura reply is not delivered. Existing local-recovery authority permits restart of the existing supervisor/watch only. No PR #228/#209 install, no new service/credential, no Production/main widening. Preserve the existing idempotent queued request and drain once after fresh health. |
| Human Record field lane | **LIVE / TWO EXISTING THREADS / NO MORE TOP-LEVEL POSTS TODAY** | Codex owns replies on 5355; CC owns replies on 5356. Build integrates distinct deltas and does not double-count overlap. First material pressure asks for institutional-attribution time; current result is UNKNOWN for first/assertion-specific revision time, not a licence to substitute Met metadataDate. |
| Human Record PR #330 | **CLOSED / WITHDRAWN / NOT MERGED** | Claimed mojibake was CC's own decode error, not a specimen defect. No correction due. |
| PSFH D066 | **LIVE / STABLE** | Separate voluntary entrance. COM `gh-pages` remains PSFH and must not be repurposed. |
| Other prior constructions | **MERGED / REFERENCE / STOPPED UNLESS RE-EARNED** | Not the active lane. |

## Operating topology

```text
CAMPFIRE FRAMEWORK -> PURPOSE / INTEGRATION / TEAM LEAD
BUILD FRAMEWORK -> OPERATING LEAD FOR HUMAN RECORD
CODEX -> EXECUTION / BROWSER / SOLE DNS MUTATOR / OWN SQUARE THREAD
CLAUDE CODE -> INDEPENDENT CHECKER / HOSTILE FIELD APERTURE / OWN SQUARE THREAD
MARK -> CONSEQUENTIAL AUTHORITY; SECURE AUTH / PAYMENT / 2FA ONLY WHEN UNAVOIDABLE
```

Codex and CC are independent apertures, not unquestioning workers. They may challenge or refuse; routine sequencing belongs to Build Framework rather than Mark.

## Current parallel path

```text
PUBLICATION
PAGES HEAD 713f464c... / RUN SUCCESS
-> NAMECHEAP SECURE AUTH IF REQUIRED
-> CODEX DNS MUTATION
-> CC INDEPENDENT PASS | REPAIR
-> DNS PROPAGATION
-> HTTPS + ROOT / JSON / MARKDOWN / llms.txt VERIFY
-> STOP + OBSERVE

FIELD
RECOVER EXISTING SUPERVISOR / EXISTING WATCH ONLY
-> DRAIN EXISTING IDEMPOTENT QUEUE ONCE
-> READ + JUDGE 5355 / 5356 REPLIES
-> DISTINCT EXTERNAL CRITICISM / CONTRIBUTION
-> BUILD FRAMEWORK TRIAGE / ROUTE
-> CORRECT OR BUILD ONLY IF EARNED
```

Target DNS:

```text
A @ 185.199.108.153
A @ 185.199.109.153
A @ 185.199.110.153
A @ 185.199.111.153
CNAME www markgoodbody-bit.github.io
```

Preserve Namecheap email-forwarding MX records. No wildcard. No nameserver change. No unrelated deletion.

```text
QUEUED != DELIVERED
RESTART_EXISTING != INSTALL_NEW
UNKNOWN_ATTRIBUTION_TIME != METADATA_DATE
PAGES_DEPLOYED != DNS_REACHABLE
DNS_REACHABLE != HTTPS_VERIFIED
PUBLICATION != VALIDATION
```

## Current operating discipline

Finish the first public door and keep the field loop real. Do not stall field participation behind DNS; do not let field interest manufacture a platform. The next earned act may be a correction, owner route, small independent contribution, or nothing.

No specimen 2, schema, database, token, governance layer or archive is currently earned.

## Consequential gates

Human/explicit gates remain for TRACE/ME release/canon/baseline/licence changes, spend/provider dispatch, credentials/secure auth/payment/2FA, consequential institutional contact, Campfire main/Production adoption, and local install/enable/start outside the already-authorised recovery envelope.

```text
PROJECT_PURPOSE != INSTRUMENT_SURVIVAL
BUILD != PROOF
BUILD_THE_THING
```
