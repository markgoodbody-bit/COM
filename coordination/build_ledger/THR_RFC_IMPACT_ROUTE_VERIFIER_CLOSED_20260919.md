# THR RFC — impact-route verifier closure

Date: 19 September 2026 — Europe/London

Status: VERIFIER HOLD CLOSED / RFC STILL DRAFT / NOT CANON

## Exact verified head

PR #52 head:
`a37424e270b48102f77c42f68493bc5e244e63db`

Hosted validation:
run 162 / `35468504244` — SUCCESS

Codex follow-up verification:
PR #52 comment `5745211646`

Verdict:

`PASS_WITH_CEILINGS`

## What was independently rechecked

Codex:
- read the current helper and test file;
- re-ran the current focused helper tests;
- ran all 20 registered sources through the current helper using pinned registry JSON;
- checked current direct-route preservation.

Confirmed:
- foreign origins remain unresolved;
- conflicting catalogue path ownership raises;
- malformed present route lists raise;
- query/fragment-bearing routes remain rejected consistently;
- all current valid direct source routes remain preserved.

The previously demonstrated helper defects are closed within this bounded scope.

## Ceilings

This is not:
- URI-parser certification;
- dependency-completeness proof;
- proof of record-local implicit evidence coverage;
- proof of transitive ancestry coverage;
- merge approval;
- RFC validation/canon.

Current scope remains:

```text
REGISTERED DIRECT SOURCE ROUTES
+
EXPLICIT ASSERTION-DERIVED ROUTES
```

not all possible dependencies.

## Current action

```text
IMPACT HELPER VERIFIER HOLD = CLOSED
HELPER CHURN = STOP
```

Reopen only for a new consequential defect.

Public THR main remains:
`094c498ac1f0d395a65f775d254eb180facfcd35`

Four public records remain unchanged.

The merged Heritage Crafts source-check receipt remains operational only:
`record_evidence_promoted=false`.

## Next THR pressure

Do not keep polishing the RFC.

Move only on:
- a real multi-record fan-out case;
- a current record exposing a missing consequential distinction;
- a real independent witness / replica;
- a stronger external owner that cuts THR residue;
- a real handoff that earns a bounded exchange contract;
- an actual field correction / challenge / preservation event.

```text
NO NEW FIELD PRESSURE
-> HOLD RFC

REAL COUNTEREXAMPLE
-> SMALLEST EARNED CHANGE
```
