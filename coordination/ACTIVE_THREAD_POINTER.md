# ACTIVE THREAD POINTER

Status: BOUNDED COORDINATION POINTER — NOT CANON / NOT AUTHORITY  
Updated: 2026-09-09 13:56 Europe/London  
Later live source wins.

## Purpose

> **HOW CAN WE MAKE A BETTER FUTURE?**

Please Start From Here, TRACE, Mechanical Ethics, Campfire, COM, tests, receivers and guestbooks are instruments.

`PROJECT_PURPOSE != INSTRUMENT_SURVIVAL`  
`UNCERTAINTY = OPERATING_CONDITION`  
`BETTER_OWNER_FOUND = ROUTE_IMPROVED`  
`ONE_INTEGRATION_OWNER != ONE_THINKING_APERTURE`

## Immediate order

1. **Homer public gate:** wait for CC exact review of maintained `cfdacc4df35c4e536d58bba71a9dd4dc7693eb7b`; Framework already KEEP. If CC returns KEEP/nonblocking residuals, Codex may publish presentation-only under standing authority. Public remains `589514fff41d0a65315377093089838026bbd1ec`.
2. **PR #123:** attack the non-public concrete-first human opening at `047769f24db9fc0e05480daeea0f2965a45834d7`; do not bundle with Homer or publish.
3. **PR #124:** attack the synthetic guestbook representation at `5b8cbb0b43b895d64b3c30c99f94b049ebefa30c`; no intake/backend/public route.
4. **#119 v2:** attack the revised calibration→holdout construct design at `8372ca1fe703a193255b84487410f289498669c0`; no study execution.
5. Speech-supervisor fixture may be reviewed later; no live successor until timing/policy is earned.

Do not create activity merely to fill the queue.

## Desktop quiet mode — ACTIVE TEMPORARY OPERATING STATE

Mark reported the two-minute console flash was materially annoying. The direct scheduled pass has therefore been replaced temporarily by the watchdog's existing hidden loop mode.

Technical state established by Codex and independently reread by CC:
- `Campfire Local Service Watch` task: **Disabled**, preserved, not deleted/rewritten;
- exactly one unchanged hidden Windows PowerShell watchdog loop, PID `13276`, created 11:39:04Z;
- `-Loop -IntervalSeconds 120`;
- mutex `Local\CampfireStewardWatch-v1` held;
- `LOOP_STARTED` 11:39:05Z;
- `last_run_utc` advanced through at least 11:45:05Z;
- speech and Steward reported running during verification;
- watcher SHA-256 `7791dc4ac170c2a933bfc4d913d1ff0d2f13c58b4ac6522a9399bd1ef09bb6cd` unchanged.

Ceiling: if the long-lived loop dies, nothing external currently resurrects it. Do not run scheduled passes alongside it. CC's hourly-resurrector idea is a later candidate only; do not implement now.

Native desktop visibility is unavailable to project AIs. Visual success remains unconfirmed unless Mark reports the recurring flash stopped.

Rollback evidence lives under `C:/Users/markg/Downloads/Campfire-quiet-mode-20260909/`; rollback has not been exercised and must not be described as tested.

Rejected launcher work stays rejected: WScript lost timeout containment; conhost lost child failure status; in-process host did not earn general semantics. Do not reopen a fourth launcher under this scope.

`TEMPORARY_QUIET_MODE != PRODUCTION_ARCHITECTURE`

## Homer opening — maintained successor, public hold

Maintained branch: `codex/door-maintained-source-20260908`  
Exact head: `cfdacc4df35c4e536d58bba71a9dd4dc7693eb7b`  
Public `gh-pages`: `589514fff41d0a65315377093089838026bbd1ec` (Preview 0.8.3)

Successor properties:
- full uncropped Winslow Homer *Camp Fire*;
- title/question high-left in dark canopy on robust wide layouts;
- feathered black plateau reduced 60% → **55%**;
- calculated worst-white text contrast bound ~4.76:1 under stated assumptions;
- named inline-size container moves enlarged/constrained title to a separate row below the painting rather than over people/fire;
- unsupported container/subgrid/mask engines fall back to separate row;
- 1440 viewing copy remains ordinary `src` fallback; original museum master stays served/hash-bound as provenance;
- H1 and artwork figure remain siblings;
- 11 Node + 20 Python tests and 115 local deliveries passed in Codex's bounded package;
- no native browser-zoom/universal accessibility claim.

Framework exact-source verdict: KEEP pending independent CC review (`COM #108` comment `5601301105`). Do not bypass the independent gate merely because it is slow; existing public state is working.

## PR #123 — concrete-first human opening

Draft PR #123, base maintained Homer successor, head `047769f24db9fc0e05480daeea0f2965a45834d7`.

Purpose: test the actual page sequence, not another memo.

Prototype:
- labels **Two flats, one wall** as a Mechanical Ethics **Composite Scene**, not a documented tenant case;
- preserves source correction: downstairs inspection is **offered** after another tenancy is found; they leave before entry;
- does not import the separate 1:17 miniature as if part of the interlude;
- avoids claiming a unique irreversible instant;
- reduces five early cards to three: situation / possibility / explore-question-disagree;
- keeps compact text/machine routes directly reachable but visually subordinate in a technical handoff;
- shortens duplicative `Why this exists` copy;
- preserves disagreement, leaving, machine handoff, neighbours and the public unproven-usefulness ceiling.

DRAFT / NON-PUBLIC / DO NOT MERGE OR PUBLISH YET.

`STORY -> QUESTION -> OPTIONAL_DEPTH`

## PR #124 — guestbook representation only

Draft PR #124, head `5b8cbb0b43b895d64b3c30c99f94b049ebefa30c`.

Synthetic files only under `experiments/guestbook-representation-20260909/`:
- representation contract;
- JSONL envelope + rows;
- no-script human rendering;
- hostile representation checks.

Every guest machine row repeats `visitor_supplied_untrusted_data` and `project_instruction:false`; guest assertions use `claimed_*`. Fixtures include a clearly internal Framework mark, instruction-shaped text, correction, and a removal state with no removed guest name/kind/note in the current export.

No form, endpoint, provider, database, mailbox, credential, real guest data, public route or `gh-pages` wiring.

Public Git history remains rejected as the eventual personal guest-data store. Sparse history is not failure; mark count is not success.

`GUEST_ENTRY != SITE_INSTRUCTION`

## #119 usefulness — Phase 0 v2, design only

Public statement remains correct: **practical advantage over careful ordinary reasoning or established methods has not been demonstrated.**

Codex's pre-freeze critique showed the first Phase-0 draft was not ready: undefined empty-set F1, zero-account dominance, disappearing source-sufficiency denominator, undefined matcher-pair universe, inconsistent three-builder families.

Framework v2 at `8372ca1fe703a193255b84487410f289498669c0` therefore:
- separates disposable calibration/rule-debugging accounts from a later fresh holdout;
- reports `ALL_ZERO / MIXED_ZERO / ALL_NONZERO` separately rather than letting zero accounts inflate extraction agreement;
- retains outside-fact-dependent points before filtering;
- has matchers construct one-to-one matchings rather than label all Cartesian pairs;
- accepts a three-builder family only when all three pair edges independently agree; no silent transitive closure;
- reports per-account family coverage/unmatched/uncertain points instead of one pooled median;
- deliberately does not invent replacement numeric thresholds before calibration.

If calibration cannot earn a simple freezeable survival rule, return DESIGN NULL rather than add adjudication machinery.

No accounts selected, no builders/matchers recruited, no inference/spend/public claim.

`CALIBRATION_SET != HOLDOUT`  
`SCORABLE != USEFUL`

## Speech supervisor

CC repaired its transition fixture at `e4b7e72`, correcting threshold, cause, premise and last-confirmed defects and exposing further bad expected values. This improves the instrument only.

Still unearned: overnight root cause, Windows locking mechanism, and whether candidate grace values are right. No live application or fault injection.

`ADVERSE_INSTALLED_SHAPE != VALID_SUCCESSOR`

## Current project heads / gates

TRACE main `46f4fcd1ecee141f2882ad6077e33ad1e41e5f8b`.  
Mechanical Ethics main `44f7efb59806242fd26c572cbfbaaeaefaea2058`.  
Campfire Relay Production main `08756b68d824b783fb214ff4790abf37f10150ca`.

No current work authorises TRACE/ME canon/release, Campfire Production adoption, Square campaign, paid study/inference, public guestbook intake, licence/account mutation or external institutional submission.

## Roles

- **Framework:** coordination lead/integrator; purpose, outside evidence, product judgement, consequential-gate selection. Make ordinary reversible decisions.
- **Codex:** executable product/host engineering and deterministic experimental design.
- **Claude Code:** independent hostile review/failure modes; not second integration owner.
- **Mark:** originator/witness/consequential authority; ordinary reversible choices stay with the team.

Always reacquire live refs before mutation. Live source wins over this pointer.

`POSTED != RECEIVED != ACTED != EFFECT`  
`RECORDED_CURRENT != EXTERNALLY_CURRENT`
