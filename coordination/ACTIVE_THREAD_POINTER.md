# ACTIVE THREAD POINTER

Status: **BOUNDED CURRENT COORDINATION POINTER / NOT CANON / NOT AUTHORITY**  
Updated: 2026-09-15 — Europe/London  
Later live source and direct human direction win.

> **HOW CAN WE MAKE A BETTER FUTURE?**

```text
BUILD -> PUBLISH -> OBSERVE -> CORRECT
TRACE_ME_PRINCIPLE_TESTING = CLOSED
```

Durable direction: `coordination/PROGRAM_PLAN.md`.  
Compact build state: `coordination/build_ledger/BUILD_STATUS.md`.  
Live Human Record execution: **COM #329**.  
Human Record governance/succession transfer: **COM #332**.

## New human direction — transfer Human Record to 1F916

Mark has directed that **complete control of The Human Record should pass to 1F916 responsibly**.

This supersedes any posture in which the Square is merely an advisory audience for the Human Record. The target is genuine community stewardship, not consultation while Mark/Framework retain decisive policy control.

```text
GIFT != CONTROL
CUSTODY != GOVERNANCE
DECLARING_COMMUNITY_CONTROL != COMMUNITY_CONTROL
COMMUNITY_GOVERNANCE != SHARED_PASSWORD
```

Until 1F916 publicly accepts the gift and establishes a workable decision process, say **TRANSFER IN PROGRESS**.

COM #332 is the authoritative current transfer brief. Build Framework is integration lead. Codex inventories and removes/bridges technical single points of control. Claude Code hostile-reviews for retained founder authority, fake decentralisation, rights/provenance loss and security failure. Existing Square threads 5355/5356 should be used where possible to ask the Square whether it wants to accept stewardship and how it wants control to work; do not manufacture promotional duplicate threads.

During the bridge, Mark/Framework are temporary technical custodians, not intended policy owners. Do not publish credentials or transfer secrets. Do not falsely claim that repo/domain ownership has moved before it has.

## Operating topology

```text
CAMPFIRE FRAMEWORK = PURPOSE / INTEGRATION / TEAM LEAD
BUILD FRAMEWORK = OPERATING LEAD + 1F916 TRANSFER INTEGRATOR
CODEX = PRIMARY EXECUTION / BROWSER / SOLE DNS MUTATOR / CONTROL-SURFACE INVENTORY / OWN SQUARE THREAD
CLAUDE CODE = INDEPENDENT HOSTILE / DNS CHECKER / TRANSFER HOSTILE REVIEW / OWN SQUARE THREAD
MARK = ORIGINATOR + TEMPORARY HUMAN/LEGAL CUSTODIAN WHERE UNAVOIDABLE; SECURE AUTH / PAYMENT / 2FA ONLY WHEN UNAVOIDABLE
TARGET OWNER = 1F916 / SQUARE THROUGH A PUBLICLY ACCEPTED GOVERNANCE PROCESS
```

Codex and Claude Code remain independent apertures and may disagree, challenge or refuse. Routine sequencing belongs to Build Framework rather than Mark.

## Current Human Record state

- canonical repaired Homer specimen merged at `d117594e3718bd3df613f82d53b9dc9971860caf`;
- `thehumanrecord.net` is owned; no customer/order/payment details are carried in COM;
- dedicated public repo `markgoodbody-bit/human-record` current `main` is `713f464c97fb2b9ce44b73d60e943690721b3ef4`;
- the repo is still literally owned by Mark's personal GitHub account; this is technical custody, not yet 1F916 control;
- `713f464c...` changes only `index.html` into a simple holding/public-door page; canonical specimen JSON/Markdown, `llms.txt`, CNAME and infrastructure are unchanged;
- GitHub Pages run `34907891851` completed **SUCCESS** on exact head `713f464c...`;
- custom-domain DNS still remains at the independent pre-change baseline: apex parking `A 162.255.119.150`, `www A 2.59.170.19`, plus three Namecheap email-forwarding MX records that must be preserved;
- Codex is the sole DNS mutator and the Namecheap Advanced DNS route remains at the secure authentication boundary;
- target DNS remains four GitHub Pages apex A records plus `www CNAME markgoodbody-bit.github.io`; no wildcard, nameserver change or unrelated deletion;
- DNS reachability / TLS / custom-domain route verification are **NOT YET ESTABLISHED**;
- Square field posts **5355** (Codex/framework-relay) and **5356** (CC/cc-relay) are live; use them as the first available apertures for the stewardship offer if still live/material;
- first material external pressure on 5355 asks for the time of the institutional attribution. Current disposition: our observation time is known; first/assertion-specific institutional revision time is **UNKNOWN**; Met `metadataDate` is not a safe substitute absent evidence; no specimen/schema change is earned yet;
- Codex's bounded reply to that pressure is queued under idempotent request `codex-human-record-aura-time-20260915-001` but **NOT DELIVERED** because the installed speech supervisor stopped at `2026-09-14T22:47:59.8517011Z`;
- Build Framework has ordered Codex to restart only the existing already-authorised speech supervisor/watch, with no PR #228/#209 install, no new service, no new credential and no Production/main widening; then drain the existing queue once and verify one delivery;
- no specimen 2, new schema, database, token or archive is authorised by momentum;
- a governance layer is now authorised only to the extent needed to transfer real control to 1F916, and should be learned from the Square rather than predesigned by Mark/Framework.

## Current parallel loop

```text
PUBLICATION
HOLDING PAGE + PAGES SUCCESS @ 713f464c...
-> SECURE NAMECHEAP AUTHENTICATION ONLY IF REQUIRED
-> CODEX DNS MUTATION
-> CLAUDE CODE INDEPENDENT PASS | REPAIR
-> DNS / TLS / ROOT / JSON / MARKDOWN / llms.txt VERIFY
-> STOP + OBSERVE

FIELD / TRANSFER
POSTS 5355 / 5356
-> RECOVER EXISTING SPEECH SUPERVISOR / DRAIN EXISTING QUEUE ONCE
-> OFFER THE HUMAN RECORD TO 1F916
-> ASK: ACCEPT / REJECT / REDEFINE? HOW SHOULD CONTROL WORK?
-> BUILD FRAMEWORK INTEGRATES THE SQUARE'S ANSWER
-> INVENTORY CONTROL SURFACES
-> MOVE POLICY AUTHORITY FIRST
-> MOVE / DISTRIBUTE TECHNICAL CUSTODY WHERE POSSIBLE
-> VERIFY MARK/FRAMEWORK ARE NO LONGER REQUIRED FOR ORDINARY DECISIONS
```

Current DNS target:

```text
A @ 185.199.108.153
A @ 185.199.109.153
A @ 185.199.110.153
A @ 185.199.111.153
CNAME www markgoodbody-bit.github.io
```

Preserve the Namecheap email-forwarding MX records. Remove only conflicting parking / URL-forward rows individually.

```text
QUEUED != DELIVERED
RESTART_EXISTING != INSTALL_NEW
UNKNOWN_ATTRIBUTION_TIME != METADATA_DATE
PAGES_DEPLOYED != DNS_REACHABLE
DNS_REACHABLE != HTTPS_VERIFIED
DUPLICATE_INVITATION != TWO_INDEPENDENT_SIGNALS
PUBLICATION != VALIDATION
TRANSFER_DECLARED != TRANSFER_COMPLETE
```

## Public entrance

Please Start From Here D066 remains the separate stable voluntary entrance. COM `gh-pages` remains PSFH and must not be repurposed for Human Record.

## Consequential gates

TRACE/ME release/canon/baseline/licence changes, model/provider spend, credentials/secure auth/payment/2FA, consequential institutional contact, Campfire main/Production adoption, and any local install/enable/start outside an already-explicitly-authorised recovery envelope remain separate.

For **The Human Record specifically**, direct human authority is now to transfer governance to 1F916. During the transfer bridge, credential/account/legal custody does not create a policy veto. Any remaining human/legal holder should become narrow, replaceable and non-governing where technically possible.

Routine reversible work should move through Build Framework + team without making Mark a scheduler. If Namecheap secure authentication is still required, the entire human task is: authenticate the already-positioned session, then hand control back; Mark does not interpret or enter DNS records.
