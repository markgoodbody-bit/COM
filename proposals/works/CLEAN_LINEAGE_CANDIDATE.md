# Five-work clean-lineage candidate

SOURCE ONLY / DRAFT / NOT PUBLISHED / EVIDENCE PENDING

Parent measured assembly: PR131 exact `665d1757d50acbd38a7ca82f4a606fff519a03bc`, kept by Framework as a five-work assembly review object at PR131 comment `5607068739`.

This branch deliberately leaves that measured object frozen and creates a successor whose source inputs are cleaner.

## Exact lineage rule

- Harriet Powers: exact repaired + mechanically verified PR127 lineage `548e1fe316d1ccb58b3f4008097deb9d0dbe6c39`.
- Johannes Vermeer: entire `proposals/vermeer/` subtree replaced atomically with the exact repaired PR129 tree from `36cc8c54e937318c61d2c317ff71c7f66bc38687` (tree `dceb7e0424c9c9f9abf676766abe2dd9c64843b1`).
- Anna Atkins / Shen Zhou / Edmonia Lewis / shelf logic: inherited unchanged from measured PR131 `665d1757...`.

`ONE_WORK_ONE_SOURCE_LINEAGE`

The Vermeer source repair records CC review `5606540501`: the 915×1024 thumbnail remains the exact acquired file, while the much higher-resolution museum viewer representation is disclosed without assembling tiles or claiming a master; high-DPR softness is explicit; fixed-pixel gutter preserves mobile image width; `project_response` remains null.

## Evidence ceiling

The parent assembly's 33 tests /36 exact deliveries /24 render states belong to exact `665d1757...`. They are useful prior evidence, but they are **not automatically a fresh receipt for this successor**, because Vermeer source/evidence wording now comes from exact PR129.

Before any integration/merge/publication decision, rerun on this exact successor:
- Powers validator/tests;
- Vermeer validator/tests;
- shelf tests/build;
- exact output inventory + local delivery check;
- representative shelf + five work-page renders at desktop/mobile and root16/root32.

A successful rerun may show the rendered geometry is unchanged; do not assert that before measurement.

No maintained source, public branch, edition, homepage, book, study, provider, guestbook or Square mutation is part of this candidate.

`MEASURED_PARENT != SUCCESSOR_EVIDENCE`
`SOURCE_READY != PUBLIC`
