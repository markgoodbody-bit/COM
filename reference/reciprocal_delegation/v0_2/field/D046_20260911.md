# Field trace — PSFH D046, 11 September 2026

Status: **OBSERVED PROJECT OPERATION / REFERENCE-MODEL INPUT / NOT VALIDATION / NOT PRODUCTION ADOPTION**

This trace records an actual project sequence that occurred before this v0.2 repair was written.

## Trigger

Mark supplied a fresh external-Claude cold read of the live PSFH entrance. The text is preserved separately at:

`evidence/PSFH_EXTERNAL_CLAUDE_COLD_READ_20260911.md`

It was not Claude Code output.

Framework checked the findings against the maintained source and accepted a bounded D046 repair:
- put the existing *Two flats, one wall* story before route machinery;
- surface the worked-revision route without calling it efficacy evidence;
- reduce repeated practical-advantage disavowal;
- make the GitHub-account/no-account reply boundary legible;
- add no form/backend/intake surface.

## Source lane

Framework initially delegated D046 implementation to Codex on branch `codex/d046-story-first-door-20260911` from maintained D045 head `0eef661490f86d7a3ce1c2fe995d3c47eb05ecbc`.

The branch did not move. Framework then explicitly took over the bounded source edit rather than running a second concurrent mutator. D046 source PR #215 changed four source files and merged to maintained source:

`852206af720171e2e426df44c26701c30ee5d9a1`

This preserves the `single_mutator` principle as a temporal handoff, not a permanent role monopoly.

## Publication lane

Framework dispatched the exact maintained head to Codex with an explicit return contract:
- final maintained head;
- public `gh-pages` head;
- exact output delta;
- static build result;
- root bytes and SHA-256.

Codex published through the established static lane and returned:
- final maintained `8e27856f2397779ab7cd65f882490a70fa6b2eef`;
- public `d7e98da7bcf0a01ae6566de22d1e75b2a6313ccf`;
- 11 changed / 144 byte-identical outputs / 0 added / 0 deleted;
- root 21,045 bytes;
- root SHA-256 `1f6f36d323e622ec0d4a9d29998ce4df983fc8e82d428a1b6870e29a19788eb0`;
- Pages built exact public head with no deployment error.

Claude Code then independently read the canonical origin and matched the served root byte-for-byte to the public Git blob.

## Why v0.1 could not truthfully encode this publication

The publication was public but was still ordinary reversible PSFH work under Mark's standing build/publish direction. It did not cross the project's consequential gates for account/settings, spend, credentials, TRACE/ME release/canon/licence, institutional contact, Square actuation or Campfire Production adoption.

So:

`PUBLIC != CONSEQUENTIAL`

No request-specific consequential approval token was used.

But v0.1 also required a numeric human-correction minute window for every public mutation. No such numeric window had been declared. Assigning one after the fact would make the record cleaner by making it less true.

The actual initiative bound was one public publication action followed by a mandatory evidence receipt and lane hand-back.

That is why v0.2 adds an event-bound correction mode.

## What this does not establish

- D046 does not prove the reciprocal-delegation model is useful in general.
- One successful publication does not widen Codex authority.
- The event-bound form may fail in other public or high-tempo situations.
- A receipt is not human review or agreement.
- Public routine work can still become consequential because of context.
- This trace does not authorize Campfire Production adoption.

## Field remainder

The reference now needs to preserve both truths:

1. initiative must stay inside practical correction capacity;
2. the record must not manufacture a precision the operating arrangement did not possess.

`CORRECTION_BOUND != FICTIONAL_CLOCK`
