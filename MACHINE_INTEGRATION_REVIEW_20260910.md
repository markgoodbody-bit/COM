# Machine entrance and index: current-base integration

10 September 2026. Codex source review and local execution, not independent reader validation.

Base maintained `4bf08436a917ab2c31881a035fa223fc57bbfb6b`; preceding public `7e6ea75c8040feb891dd725a5acad6bcaef9eb82`. Separate reviewed proposals: PR156 `ed45097e22fa392515a602f5027dbef25e3eded3` and PR157 `9c3872910170a02e6306a00db81aa6e4ae40376a`. Both histories are merged locally; the adjacent source-pin conflict was resolved by retaining BOTH new hashes, not choosing a file wholesale.

## Before and after

PR156 changes only greeting.question, greeting.invitation and reading in start.json. Arrival no longer asks a receiver to supply a purpose; one bounded use, challenge, another method and stopping remain possible. Routes, reply boundary, format, value choice and scope are unchanged. This is optional reference wording, not page-created authority or a comprehension test. The repeated phrase about one complete use is a remaining concision concern, not a reason to invent another protocol. The legacy field name greeting.question now holds a statement; the schema is preserved, not presented as a newly designed one.

PR157 puts the existing summaries first, removes the duplicated root six-question prose and links to the existing homepage section. All previous hyperlinks survive; exactly one is added. The homepage uses equivalent human-facing prompts, not byte-identical wording. Its six operations are not the ten graph-node questions. Root index: 6,258 to 5,383 UTF-8 bytes, with 305 whitespace-separated words before Start (not the discarded PR152 candidate's 474). Explore index remains 3,535 bytes. Root and Explore retain their older conversational greetings; no semantic harmonisation was hidden in integration.

The source-view edition descriptions were corrected: the revised arrival is no longer attributed to the old Preview0.7 publishing commit. D022 records the combined publication scope and predecessor; earlier history is preserved. Manifest date meaning, homepage, image-first layout, graph, two rooms, scripts, crawler policy and fixed TRACE/ME sources are unchanged. Significance is a separate future experiment.

## Evidence

Normal build passed. 28 Node checks and 16 Python checks passed across the existing suites and three targeted integration checks. An old first-contact assertion initially failed because it required the intentionally removed duplicated heading; it now checks the single explicit link and the unchanged full six-question destination. The initial failure is not a product failure silently suppressed: the changed expectation follows the reviewed move from duplication to linking.

The integration checks preserve both exact pins and allow only the three reviewed JSON fields to differ. Source-view HTML roundtrips retain complete raw text; changed/invalid sources remain rejected. All 61 delivery-map entries match generated bytes. Output comparison against the immediate public parent permits exactly nine changed resources and requires the other 146 of155 to remain byte-identical. Changed: root llms, Explore llms, start JSON, two HTML source views, map, manifest and paired history.

Bounded browser check on the owned local preview: direct `/#small-loop` arrival opens the full introduction, exposes all six labelled questions and has no horizontal overflow at the current viewport. No new visual design or mobile-layout claim is made. HTTP HEAD on the local homepage returned200 before preview handoff. No direct public-origin verification was performed or bypassed. Public-Git parity and Pages deployment require their subsequent receipt.

## Disposition and limits

KEEP both proposals for routine reversible integration. Neither is evidence of improved agent continuation, understanding, provider consumption, external validation or new permissions. No ten-room conversion, PR130 re-entry adoption, service mutation, tracking or crawler/training decision follows. Old PR152 failure and checksum-repair evidence remain in PR152 and COM5620430056; no old-base output is used here.

CC5620427868's repeated missing-script-hash claim was corrected against the explicit published manifest in COM5620743838. D020/D021 did not change homepage bytes. The separate concern about visible build identification and D017's absent explicit predecessor remains a limitation, not a reason to alter historical entries or label a broad edition false.
