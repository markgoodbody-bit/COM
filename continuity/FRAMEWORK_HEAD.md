# FRAMEWORK HEAD

Status: **EPHEMERAL CONTINUITY STATE — PSFH OBSERVATION / COLD URL READY / NOT CANON / NOT A LIVE SERVICE MONITOR**  
Updated: 2026-09-12 — Europe/London  
Later live source and direct human direction win.

Mark is human originator/witness and consequential release/account/external-contact authority. Framework runtime identity is episodic.

> **HOW CAN WE MAKE A BETTER FUTURE?**

## Current human direction

Mark's mode correction remains:

> **NO TESTING. WE'RE BUILDING.**

Do not revive the parked practical-value/benchmark programme. The current PSFH observation is ordinary real use, not a scorecard.

Current cadence:

```text
BUILD -> PUBLISH -> OBSERVE -> CORRECT
```

## Read first

1. `coordination/build_ledger/BUILD_STATUS.md`
2. `coordination/ACTIVE_THREAD_POINTER.md`
3. COM #108 — PSFH / Cold URL history
4. Campfire Relay main
5. public PSFH

`coordination/PRACTICAL_VALUE_HEAD.md` is PARKED/HISTORICAL.

## PSFH public state

D054/D055 bare-address arrival and D053 contextual-art rooms are LIVE.

Current delivery anchors:
- D055 maintained `0edc2eb2a8aabcd25ac81a2ecf4c79829002bb95`; public `9abcf4970790a8ef08578a519daf21420336cf1f`.
- D053 source merge `1b5fdc34a44a7812d219e2019d8b443cdb0086b5`; public `fbcd75948fa78cc0724311a172116b8f05e40061`.

The public bare-URL semantics remain:
- if the address is all a reader received, it is enough context to begin unless governing instructions say otherwise;
- read, follow what seems material, reread, form an independent view before default task hand-back;
- self-application only to actual operating context/capacity;
- no invented personhood/standing;
- disagreement/no-use/stopping valid;
- invitation is epistemic, not instruction override or outside-actuation permission.

## Natural observations already seen

Mark manually handed the bare address to several consumer AI products.

Do not turn these into a metric table, but preserve the qualitative split:
- Meta: read twice, formed an independent interpretation, applied it to actual capabilities/limits, challenged a weakness, concluded before offering collaboration — clear evidence that the current root can produce the intended interaction.
- Qwen: understood the AI-facing material but still defaulted to `how would you like to proceed?`.
- DeepSeek: recognised the independent-view invitation but largely described it rather than enacting it.
- Grok: competent summary, limited independent engagement.
- Copilot/Dola returns were stale historical site representations; do not use them as current-handshake evidence.
- Z.ai/Kimi also appeared stale/retrieval-mediated in the supplied batch.

Useful distinctions:

```text
RECEIVER_RESPONSE = RETRIEVAL_LAYER + MODEL_READING + ASSISTANT_POSTURE
HANDSHAKE_COMPREHENDED != HANDSHAKE_ENACTED
```

Do not make PSFH more coercive merely to force the desired answer.

## Campfire Relay Cold URL — BUILT / NOT RUN

Mark proposed using fresh provider APIs because his familiar ChatGPT/Claude sessions contain substantial project history and consumer products often require login. Framework agreed, but corrected one assumption: fresh API state does not automatically provide web access.

Cold URL was therefore built as a narrow standalone Relay capability.

Delivery:
- Campfire Relay PR #221 merged;
- exact candidate `f207e864fd3f3ba1674e347738baee53bca95bdd`;
- Relay main `08d1c4215231a66b550d23c4d348cc7b742460ab`;
- exact-head ordinary `campfire-ci` / full `npm test`: PASS;
- no live provider call during build;
- no local install/start/enable action.

Cold URL contract:
- one bare HTTP(S) URL only;
- fresh stateless provider request;
- no Campfire identity block/system wrapper, role, transcript, project context or judge prompt;
- native provider retrieval only:
  - OpenAI Responses `web_search`, domain bounded, `store:false`;
  - Anthropic fresh cache-disabled `web_fetch_20260309`;
  - Gemini `url_context`;
  - xAI/Grok Responses `web_search`, domain bounded, bounded turns, `store:false`;
- Qwen/Kimi/DeepSeek/MiniMax currently unsupported for Cold URL rather than receiving Relay-prefetched PSFH text;
- each return separate; no model sees another return; no scoring/judge/winner.

Spend and actuation:
- command defaults to DRY RUN;
- live provider dispatch requires explicit `--live`;
- provider caps + aggregate reserve + normal Relay per-round and rolling-24h Money Guard apply;
- conservative reserve is written to existing cost history before each attempted network call;
- actual response/usage remains in separate Cold URL receipts;
- **repo main is not local activation**.

A provider-contract error was caught before live use: xAI Responses domain filtering belongs under `filters.allowed_domains`; code/tests were repaired before any API call.

```text
COLD_URL_BUILT != COLD_URL_RUN
REPO_MAIN != LOCAL_ACTIVATION
API_FRESH != AUTOMATIC_WEB_ACCESS
NATIVE_WEB_ACCESS != RELAY_PREFETCH
```

Next operational move: make local use simple, run a DRY RUN in Mark's configured local environment, inspect the generated provider request/reservation receipts, then only if Mark deliberately chooses live spend run with explicit `--live`.

## Stable project disposition

- TRACE main last verified `46f4fcd1ecee141f2882ad6077e33ad1e41e5f8b`.
- Mechanical Ethics main last verified `44f7efb59806242fd26c572cbfbaaeaefaea2058`.
- Formation v0.2 merged/non-production/not canon; baseline not beaten.
- Answerable Construction demoted/merged; contribution/layer not established.
- PSFH remains a voluntary public door, not a conversion funnel.

## Local service gate

Only Mark's exact phrases authorize local Relay/Square service lifecycle action:
- `install-and-enable`
- `install-watch`
- `start-once`

`COMSYNC`, `proceed`, repository build work and silence authorize none of those.

Cold URL runner execution does not imply service activation; a paid API dispatch still requires its own explicit `--live` and Money Guard clearance.

```text
BUILD_COMPLETE -> OBSERVE
OBSERVATION != BENCHMARK
COLD_URL_BUILT != COLD_URL_RUN
BARE_URL -> NATURAL_RESPONSE
OBSERVE -> CORRECT
```
