# D031 shared human/AI entrance

Basis: FW5636263552 / PR187 source 933488b9434611b8852e315f9dc7e85c1b031c23, maintained D030 882c9c257ff1872093c0799b42bee6f21a2b87a9, publication eacf5dbef0394fbebfe41a2b3d5a4529b0131cd5.

## Before / after

The purpose now explicitly addresses humans and AI/automated readers, says reference does not override instructions, and exposes llms.txt/start.json before the seven route choices. The redundant lower machine handoff is removed; sharing and standing boundaries remain. Footer no longer implies waiting on cold readers. Supplied copy was retained.

The actual static builder had a separate hardcoded description; updating app/layout.tsx alone did not change delivery metadata. The builder description now matches the supplied metadata. D031 is paired in reader history and recorded in the manifest. No stylesheet, journey script, art, reading or machine-payload change. The removed direct HTML-source shortcut's file remains available via the catalogue/machine surfaces.

Exactly four of 155 generated files differ from D030: index.html, manifest.json, changes.md and changes.html. All other 151 files match the parent Git blobs, including style.css. Root is 20777 bytes, SHA256 d7441bfb038c3fc270bcc085b6dda263d3fedccb8a4c0621f8a21b88fe0d8057, 275 bytes larger than D030.

## Checks and limits

Normal static build passed. Seventeen relevant Node tests cover exact output scope, navigation/native structure, shared audience and generated metadata, machine integration, map preservation and exact delivery of 155 local files. Twelve first-contact Python checks passed after explicit D031 fixture repairs, including negative controls for unlisted text removal/addition. D031 paired history is included in the history check. No full-suite all-green claim; historical favicon/house-style test limitations remain outside this change.

First build attempt lacked local dependencies; reused the existing ignored dependency directory through a local junction, with no manifest/lockfile changes. Initial Python checks failed five obsolete editorial/link/description expectations. Changed only the named D031 text inventory, canonical-equivalent relative URL comparison, and the deliberately removed HTML-source shortcut. The resource itself must still exist. One diagnostic initially used the old publishing checkout with insufficient revision objects; corrected by explicitly pointing the test at this COM worktree.

Desktop screenshot inspected in the existing browser: whole painting first, purpose and invitation on the left, seven routes alongside, no overlap. At explicit 390x844 viewport, browser reports 390x845, no horizontal overflow. The new compact links end at y791/y817 and are within that viewport. This is measured layout, not a mobile visual sign-off: browser screenshot capture failed repeatedly under the override, and the optional CDP capability was unavailable. Viewport reset afterwards. No public page or browser preference was modified for QA.

Local preview with ?nojs=1 tightens only its response CSP to block scripts. data-enhanced is absent, the entrance and compact links remain in the DOM, and clicking Work changes the native fragment to #step-work with that panel displayed. Compact destinations return the exact generated bytes in the local delivery check. Browser clicking the JSON link did not provide a distinct rendered destination, so no JSON-viewer behavior claim is made. Keyboard Enter attempts did not navigate in this automation session; click execution, rather than those attempts, supports the native-route result.

No receiver experiment, source-body study, efficacy claim, canon/licence or account change. Existing GitHub Pages hosting is retained, not migrated to another platform. Publication identity and Pages build status are recorded separately in COM; local checks are not direct public-origin readback.
