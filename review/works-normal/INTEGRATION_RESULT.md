# Normal-source integration result

Source implementation: `8b17e5a`; navigation repair measurement: `465cf32`.
Framework reconciliation receipt: COM108 `5607564123`.
Reconciliation merge: `985289b520ceca379d42fc5d86bb9a608e85c0f0`.

The merge deliberately retains the tested Codex source tree and Framework's
`798a458` implementation history, not two executable generators. Framework's
receipt prefers the single small byte-pinned normal-source copier. The temporary
backup branch has no PR and is not a competing current product candidate.

## Explicit boundary

Against source-review PR132, no proposal input changed. Against the maintained
site, the normal build adds36 declared work files, one homepage Works link, an
optional machine-map route and one mobile navigation grid repair. The shelf gains
a homepage return and an explicit selection ceiling. All35 other copied work
files retain exact reviewed output bytes. Source inventory is
`scripts/WORKS_COPIES.json`; all36 copied Git blobs were checked against its pins.

The concurrent Framework generator, wrapper normalizer, extra machine-arrival,
llms and sitemap edits are not selected. They remain recoverable in merge
ancestry. The current map exposes Works as optional; no traversal/report-back,
privacy claim or gallery rebranding was added. Proposal/unpublished labels and
their noindex metadata remain in the candidate pending a public-release decision.

## Verification and correction

- Ordinary `npm run build`: PASS, offline, no proposal builder required.
-16 Node regression/custody/HTTP tests and22 existing Python tests: PASS.
- All154 output files delivered byte-for-byte, including existing routes.
-435 local references and10 HTML anchors across36 HTML pages: no missing target.
-28 JS-disabled/DPR1 layout states before the grid correction: no measured
  horizontal overflow or missing image. These checks did not catch the mid-word
  wrap of Discussion; manual viewport inspection did.
- Four fresh homepage states after the one-rule grid correction: desktop1440
  and mobile390, root16/root32. Mobile root32 now has one326px column and each
  navigation label fits intact. Normal mobile keeps two167px columns.
- Native keyboard activation verified home-to-Works, all five work-to-shelf
  returns, and shelf-to-home. This is not exhaustive focus-order testing.

`renders.json` preserves the pre-repair28-state measurements; `home-repaired.json`
supplies fresh changed-surface evidence. The work pages do not load the homepage
stylesheet and did not change during that repair. Screenshot capture is viewport
only after full-page capture failed. No physical-device, screen-reader, native
zoom, high-DPR or reader-benefit conclusion follows.

A fresh checkout of concurrent FW798a458 failed with `atkins image record SHA-256
changed`; its raw-record pins depended on checkout line endings. That failure
is not silently marked repaired in that historical implementation. The selected
public copies preserve their reviewed bytes explicitly with Git attributes.

No maintained-source merge, gh-pages mutation, service actuation or publication.

## Independent review and retained limitation

CC returned KEEP on exact `da81436228fd552fdcc392e2beb4df10eac7327b` in
PR134 comment `5607783953`, with one non-blocking limitation. CC reports comparing
16 of 16 image blobs independently with PR132 and confirming all36 declared
files are present. This is a review of that head, not a new build run or proof
of reader benefit. It does not independently reverify all36 files' provenance.

The comparison of `files` and `reviewed_files` in `test-works.mjs` checks two
declarations in the same JSON record. It cannot detect matching errors in both.
The offline copier verifies actual bytes against declared pins, but does not
resolve those pins against the historical `source_review` tree. This ceiling is
now recorded beside the inventories in `scripts/WORKS_COPIES.json`. A changed
source-review commit, inventory or copied asset needs renewed source comparison;
CC's review of da81436 must not silently become review of later changed assets.

This follow-up changes only that explanatory metadata and this review record.
It changes no pins, artwork, page, stylesheet, build logic or test assertion.
The existing test command checks selection wording; `npm run build` alone does
not run that assertion. Neither passing it nor retaining the wording proves
honest curation. Source integration remains distinct from publication, and the
unpublished wrappers remain pending a separately scoped release decision.

Follow-up verification: normal build PASS; all154 output paths and SHA-256
values unchanged from the pre-edit build; both focused works tests PASS.
No new browser matrix was needed for this metadata/documentation-only change.
