# Human arrival: reversible foyer iteration

Source basis: maintained `a5931ed238c3076e9ebf59c20cd795d135b129c9`.
Published comparison and rollback: `aed75526770de9a7c9a2aa7cef63f1167dad1669`.
Direction: COM #108 comment 5608332008. Ownership receipt: 5608441664.
Authority: Mark's direct publish-if-ready instruction and subsequent COMSYNC/proceed.

## Before / after

- Dense header with repeated title, five visible links and preview status becomes a direct map bypass plus a native expandable reading menu. Every old destination survives.
- The complete Homer image fills the viewport width. No crop, new artwork, image-byte change or decorative border. On wide screens the existing feathered contrast treatment stays behind the title; on narrow/enlarged-text screens the title sits on the page colour, not a black plate. `Here` is italic rather than a separate logo.
- Four optional native disclosures follow the painting. Each offers a direct reading/challenge route. No selection is submitted, remembered or required. The underlying introduction remains available by scrolling; this is not a wizard.
- Existing substantive paragraphs/headings remain intact. The fixed editorial delta lists eleven added blocks; tests reject unlisted loss and addition. Status moves to the footer. This is a dated visual/navigation revision within the existing 0.8.4 reference edition, not a new TRACE/ME release.

## Earned result

- Build completed. All 17 Node and 22 Python tests passed.
- All 154 output paths compared against the prior public Git tree: only index.html, style.css and the manifest's stylesheet hash differ. The remaining 151 files, including all 36 Works files and all machine reading payloads, are byte-identical.
- Local link check: 36 HTML pages, 446 local references, 14 anchors, no problems.
- Edge 152.0.4191.66, JavaScript disabled, DPR 1: desktop 1440x1000, mobile 390x844, both with root text doubled, plus dark mobile. All images loaded and no horizontal page overflow; direct bypass visible in all five states. Each inspected HTML response matched the build bytes.
- Keyboard Enter opens/closes all four doors. Direct map reaches /explore/ without a door selection; story anchor and challenge page are reachable; browser Back returns. No cookies in the test context.
- Caught and repaired white mobile title text inherited from the prior desktop selector. Final screenshots, not the initial failed appearance, are retained here.

## Provisional / unresolved

This is a wording/navigation improvement, not evidence of improved understanding or practical usefulness. The fixed tests only bound the edit and known mechanics. Doubled root text is not native browser zoom; these are viewport captures, not a screen-reader audit or accessibility conformance claim. A large painting still takes scrolling space; the immediate map and skip link are the bypasses. Native disclosure may still feel like categorisation to some readers. CC is asked to challenge capture, exit friction, art-as-persuasion and whether the map serves a competent visitor. No answer is recorded or requested from visitors.

Direct public-domain tool readback was previously refused. Deployment status and exact Git-byte custody must be reported separately from direct served-byte observation. Use the existing GitHub Pages lane; no hosting migration or new account.
